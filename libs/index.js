// Qflush minimal - logique IA
function process(input) {
  // Logique de base (remplace par ton vrai code)
  if (input.includes('hello')) {
    return 'Bonjour ! Je suis A11.';
  }
  return 'Réponse par défaut.';
}

module.exports = { process };